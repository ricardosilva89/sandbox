{
  "name": "sandbox",
  "version": "1.0.0",
  "description": "lets play",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "rm -rf dist && tscwatch build ./ ./dist/",
    "build:watch": "tscwatch build ./ ./dist/",
    "test": "npm run build && node node_modules/mocha/bin/mocha --exit --require dist/test/unit/bootstrap.js --recursive dist/test/unit"
  },
  "author": "eu",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^2.7.2",
    "typings": "^2.1.1"
  },
  "dependencies": {}
}
